@model OrderViewModel
    <link href="~/css/UserStyle.css" rel="stylesheet" />
<h2 class="Site-header">Orderöversikt</h2>
<div class="overview-grid">
    <div class="overview customer-info">
        <h3>Dina uppgifter</h3>
        <p id="Name">@Model.Name</p>
        <p id="Name">@Model.Address, @Model.ZipCode @Model.City</p>
        <p id="Name">@Model.Email</p>
        <p id="PhoneNumber">@Model.PhoneNumber</p>
    </div>
    <div class="overview payment-info">
        <h3>Betalningsmetod</h3>
        <p>@Model.Payment</p>
    </div>
    <div class="overview delivery-info">
        <h3>Leveransmetod</h3>
        <p>@Model.Delivery</p>
    </div>
    <div class="overview product-info grid-row-span-2">
        <table class="content-table">
            <tr>
                <th>Produkt</th>
                <th>Antal</th>
                <th>Pris</th>
            </tr>
            <tr>
                <td>@Model.Product.Name</td>
                <td>@Model.Quantity</td>
                <td>@Model.Product.Price</td>
            </tr>
            @if (Model.Delivery == "Hemleverans")
            {
                <tr>
                    <td>Hemleverans</td>
                    <td></td>
                    <td>250</td>
                </tr>
            }
            <tr>
                <td>Totalt</td>
                <td></td>
                <td>@Model.TotalPrice</td>
            </tr>
        </table>
    </div>

    <div class="overview div-buttons">
        <a href="@Url.Action("Index", "Order")">Gå tillbaka</a>

        <a href="@Url.Action("ConfirmOrder", "Order", @Model)">Slutför</a>
    </div>

</div>
@*<h1>Översikt</h1>

    <h2>Din beställning</h2>
    <br />
    <label>Orderdatum:</label>
    <label>@Model.OrderDate</label>
    <br />
    <label>Namn:</label>
    <label>@Model.Name</label>
    <br />
    <label>Adress:</label>
    <label>@Model.Address</label>
    <br />
    <label>Postkod & Ort:</label>
    <label>@Model.ZipCode</label>
    <label>@Model.City</label>
    <br />
    <label>E-postadress:</label>
    <label>@Model.Email</label>
    <br />
    <label>Telefonnummer:</label>
    <label>@Model.PhoneNumber</label>
    <br />
    <label>Betalningsmetod:</label>
    <label>@Model.Payment</label>
    <br />

    <table>
        <tr>
            <th>Produkt</th>
            <th>Antal</th>
            <th>Pris</th>
        </tr>

        <tr>
            <td>@Model.Product.Name</td>
            <td>@Model.Quantity</td>
            <td>@Model.ProductPrice</td>
        </tr>
        @if (Model.IsHomeDelivery)
        {
            <td>Hemleverans</td>
            <td></td>
            <td>250</td>
        }

        <tr>
            <td>Totalt</td>
            <td></td>
            <td>@Model.TotalPrice</td>
        </tr>
    </table>
    <br />





    <a href="@Url.Action("Index", "Order")">Gå tillbaka</a>

    <a href="@Url.Action("ConfirmOrder", "Order", @Model)">Beställ</a>*@











